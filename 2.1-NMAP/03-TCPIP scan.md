<span style="font-size: 16px">So, we are going to learn about the Scan type which is one of the basic type and how does it works also in detail kind of so, lets start from </span>**<span style="font-size: 16px">TCP/IP</span>**<span style="font-size: 16px"> so we already know some basic about this </span>**<span style="font-size: 16px">TCP/IP</span>** <span style="font-size: 16px"> this is a network protocol which has 5 layers unlick OSI model which has total about 7 layers the top three layers are merged on this protocol the </span>**<span style="font-size: 16px">APPLICATION, PRESENTATION and SESSION</span>** <span style="font-size: 16px">layers are merged on this protocol and they became total 5 layer protocol. This is the </span><span style="font-size: 16px">reliable way to transferring data form one device to another. </span>

<span style="font-size: 16px">The most important part of this protocol it follow 3 way of hand shake method, so it became little bit slower than than other protocols like UDP, but now we are going to learn in detail about how actually the 3(three) ways hands shake is preformed(take place). we are going to understand it by the very simple example and later we are going to  add advance terms(like IT related) for example, </span>

<span style="font-size: 16px">we can also understand it by like it is the first step of stablishing the connection before the file start transforming, which means think like this, you want to buy apple so for that you need to go to the market, so if you are using </span>**<span style="font-size: 16px">TCP/IP</span>** <span style="font-size: 16px"> then before going to the shop at first you send a message requesting to the shop owner asking for can i came for purchase of apple. If the shop owner like your request then it will also respond with the acknowledge yes i like to do that with you, and after receiving the message form the shop owner you send the final message i am coming bro wait for me(acknowledgement) then the connection is get stablished. and the communication is started between two devices.</span>

---

<span style="font-size: 16px">Lets understand it by the technical terms, So the concept is same as the shop but here comes the some  change  they are, </span>

![What is a TCP 3-way handshake process?](https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQuCk58xkbgwW9eWrG5fXgCkWKqdvZa-7rQdWghg6KwkA&s=10)![Enlarged image](https://assets.tryhackme.com/additional/imgur/ngzBWID.png)<span style="font-size: 16px">So we can also saw on the images how the three way hands shake actually work , so at first our attacker machine send SYN flag to the target to asking for request to stablish connection and then  the target machine replies with the SYN + ACK ( acknowledgement) flag in response of SYN flag response for the attacker machine. and after that the attacker machine response with the ACK flag (</span><span style="font-size: 16px">acknowledgement) and then the connection get stablished. and the further data sharing starts.</span>

---

<span style="font-size: 16px">On the way of scanning it might take two much for finding open ports and close ports because of its three way hand shake policy 🤣.Some times it might became problem also because the logs get  saved easily after using this scan which might get problem. so this is how the TCP/IP scan works and the switch we used for this scan is </span>`nmap -sT ip address` <span style="font-size: 16px">this is one of the simple scan. later we are going to learn about the SYN scan. thank you.</span>
